resources:
  pipelines:
    realtime_road_hazard_scoring:
      name: realtime_road_hazard_scoring
      libraries:
        - glob:
            include: ../src/realtime_road_hazard_scoring/transformations/**
      environment:
        dependencies:
          - -r ../src/realtime_road_hazard_scoring/requirements.txt
      serverless: true
      catalog: ${var.catalog}
      schema: artemzharkov10_bronze
      root_path: ../src/realtime_road_hazard_scoring

